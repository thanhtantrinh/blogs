@using Model.ViewModel;
@model IEnumerable<ProductsView>

@if (Model.Count() > 0)
{

    var ItemFirst = Model.FirstOrDefault();
    var catName = ItemFirst.CategoryName;
    var catID = ItemFirst.CategoryID;

    <h3 style="margin-bottom: 30px">Sản phẩm</h3>
    <div class="service-grids">

        @foreach (var item in Model)
        {
            string link = Url.Action(item.MetaTitle + "-" + item.ID, item.CategoryAlias + "-" + item.CategoryID);
            <div class="col-md-3 col-sm-4 col-xs-6 col-min-12">
                <div class="arrival-grid simpleCart_shelfItem bounceInLeft ">
                    <div class="grid-arr">
                        <div class="grid-arrival">
                            <figure>
                                <a href="@link" class="new-gri">
                                    <div class="grid-img">
                                        <img src="@Url.Content("~/Images/Products/medium/"+ item.MoreImages) " class="img-responsive" alt="" height="223">
                                    </div>
                                </a>
                            </figure>
                        </div>
                        <div class="women">
                            <div class="gr_price">
                                <h6><a href="http://demonongnghiep.web30s.vn/Dua-leo-336928">@item.Name</a></h6>
                                <span class="hide gia_km"></span>
                                <p class="item_price gia_ban">@item.Price.Value.ToString("N0") VND VNĐ</p>
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        }					
        <div class="clearfix"></div>
    </div>

}
