@using Model.Models
@using Model.ViewModel
@model List<ProductCategoryView>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layouts.cshtml";
    PagingModel pagingModel = new PagingModel(ViewBag.currentPage, ViewBag.TotalItem);
}

@section header{
    Nhóm Sản phẩm
}

<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Nhóm sản phẩm
            </div>
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <div id="dataTables-example_wrapper" class="dataTables_wrapper form-inline dt-bootstrap no-footer">
                        @using (Html.BeginForm("Index", "ProductCategory", FormMethod.Get, new { delect_action = "Delete" }))
                        {
                            <div class="row">

                                <div class="col-md-12">
                                    <div class="dataTables_length" id="dataTables-example_length">

                                        <div class="input-group" role="group" aria-label="...">
                                            <input type="text" value="@ViewBag.SearchString" class="form-control" name="searchString" placeholder="Tìm nhóm sản phẩm ...">
                                            <div class="input-group-btn">
                                                <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
                                                <a class="btn btn-primary" data-toggle="tooltip" data-placement="top" href="@Url.Action("Create")" title="Thêm mới nhóm">
                                                    <i class="fa fa-plus-circle"></i>
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                        <div class="row">
                            <div class="col-sm-12">
                                <table id="dataTables-example" class="table table-striped table-bordered table-hover dataTable no-footer">
                                    <thead>
                                        <tr>
                                            <th width="1%"><input type="checkbox" name="check[]" /></th>
                                            <th>Trạng thái</th>
                                            <th>Tiêu đề</th>
                                            <th>Nhóm</th>
                                            <th>Ngày đăng</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model.Count() > 0)
                                        {
                                            foreach (var item in Model)
                                            {
                                                <tr id="row_@item.ID">
                                                    <th class="text-center"><input type="checkbox" value="@item.ID" name="check[]" /></th>
                                                    <td class="text-center" width="5%" style="min-width:95px">
                                                        <div class="btn-group btn-group-xs" role="group" aria-label="...">
                                                            <button type="button" class="btn btn-default">
                                                                <i class="fa fa-check-circle @(item.Status != null && item.Status.Value == true ? "text-primary" : "text-muted" )"></i>
                                                            </button>
                                                            <a class="btn btn-default" href="@Url.Action("Edit", new { ID = item.ID })"><i class="fa fa-pencil fa-fw"></i></a>
                                                            <a class="btn btn-default" href="@Url.Action("Delete", new { ID = item.ID })"><i class="fa fa-trash"></i></a>
                                                        </div>
                                                    <td>
                                                        <a href="@Url.Action("Edit", new { ID = item.ID })">@item.Name</a>
                                                        <br />
                                                        <small>(Alias: @item.MetaTitle)</small>
                                                    </td>
                                                    <td style="min-width:150px"><small>@item.ParentName</small></td>
                                                    <td>@item.CreatedDate.Value.ToString("dd-MM-yyyy")</td>
                                                </tr>
                                            }
                                        }
                                        else
                                        {
                                            <tr>
                                                <td colspan="5">
                                                    Không có thông tin cần tìm
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-sm-12">
                                    @Html.Partial("_HtmlPaging", pagingModel)
                                    @*@Html.pa(Model, page => Url.Action("Index", new { page, @catid = @ViewBag.catId }))*@                                
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>


